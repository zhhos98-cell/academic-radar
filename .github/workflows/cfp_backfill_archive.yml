name: CFP Backfill Archive

on:
  issues:
    types:
      - opened
      - edited
      - labeled
      - reopened

permissions:
  contents: write
  issues: write

jobs:
  archive-backfill:
    name: Archive backfilled conference issue
    runs-on: ubuntu-latest

    if: ${{ github.event.issue.pull_request == null && (contains(github.event.issue.labels.*.name, 'backfill') || startsWith(github.event.issue.title, '[BACKFILL]') || contains(github.event.issue.body, 'Presentation title')) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Archive backfill issue
        run: python scripts/cfp_backfill_archive.py
        env:
          CFP_BACKFILL_COMMENT_PATH: ${{ runner.temp }}/cfp_backfill_comment.md
          PERSIST_CFP_LEDGER: ${{ vars.PERSIST_CFP_LEDGER }}

      - name: Write processing audit
        if: always()
        env:
          STORAGE_MODE: ${{ vars.PERSIST_CFP_LEDGER == 'true' && 'repository' || 'ephemeral' }}
          CONTENT_PERSISTED: ${{ vars.PERSIST_CFP_LEDGER == 'true' && 'true' || 'false' }}
        run: |
          python scripts/write_audit_event.py \
            --output "${{ runner.temp }}/cfp_backfill_audit.jsonl" \
            --workflow "$GITHUB_WORKFLOW" \
            --event "cfp_backfill" \
            --source-type "issue" \
            --source-id "${{ github.event.issue.number }}" \
            --storage-mode "$STORAGE_MODE" \
            --content-persisted "$CONTENT_PERSISTED" \
            --category "issue form metadata" \
            --category "conference presentation fields"

      - name: Upload processing audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cfp-backfill-audit-${{ github.run_id }}
          path: ${{ runner.temp }}/cfp_backfill_audit.jsonl
          retention-days: 30
          if-no-files-found: ignore

      - name: Commit generated CFP ledger files
        if: ${{ vars.PERSIST_CFP_LEDGER == 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add cfp/

          if git diff --cached --quiet; then
            echo "No generated changes to commit."
          else
            git commit -m "Archive backfilled conference issue #${{ github.event.issue.number }}"
            git push
          fi

      - name: Comment on issue
        if: ${{ always() && vars.COMMENT_CFP_ISSUES == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
          CFP_BACKFILL_COMMENT_PATH: ${{ runner.temp }}/cfp_backfill_comment.md
        run: |
          if [ -f "$CFP_BACKFILL_COMMENT_PATH" ]; then
            gh issue comment "${{ github.event.issue.number }}" --body-file "$CFP_BACKFILL_COMMENT_PATH"
          else
            echo "No comment file generated."
          fi

      - name: Close issue as completed
        if: ${{ success() && vars.CLOSE_CFP_ISSUES == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue close "${{ github.event.issue.number }}" --reason completed
